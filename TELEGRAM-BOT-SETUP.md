# 🤖 Настройка Telegram бота для уведомлений о деплое

Пошаговая инструкция по созданию Telegram бота для получения уведомлений о деплое.

## 📋 Шаг 1: Создайте Telegram бота

### 1.1. Найдите BotFather

1. Откройте Telegram
2. Найдите бота **@BotFather** (официальный бот для создания ботов)
3. Нажмите **Start**

### 1.2. Создайте нового бота

Отправьте команду:
```
/newbot
```

BotFather попросит:

**1. Имя бота** (название, которое видят пользователи):
```
Hakolr Deploy Bot
```

**2. Username бота** (должен заканчиваться на `bot`):
```
hakolr_deploy_bot
```
или
```
hakolr_dev_bot
```

### 1.3. Получите токен

BotFather отправит сообщение с токеном:
```
Done! Congratulations on your new bot...

Use this token to access the HTTP API:
1234567890:ABCdefGHIjklMNOpqrsTUVwxyz123456789

For a description of the Bot API, see this page: https://core.telegram.org/bots/api
```

**Скопируйте токен** (строка вида `1234567890:ABCdef...`)

⚠️ **ВАЖНО:** Никому не показывайте этот токен!

---

## 📋 Шаг 2: Получите Chat ID

### 2.1. Начните диалог с ботом

1. Найдите вашего бота по username (например, `@hakolr_deploy_bot`)
2. Нажмите **Start**
3. Отправьте любое сообщение боту, например:
   ```
   Привет!
   ```

### 2.2. Получите Chat ID

Есть два способа:

#### Способ А: Через специального бота (проще)

1. Найдите бота **@userinfobot**
2. Нажмите **Start**
3. Он пришлет ваш Chat ID (число вида `123456789`)

#### Способ Б: Через API (если нужна группа)

Откройте в браузере (замените `YOUR_BOT_TOKEN` на токен вашего бота):

```
https://api.telegram.org/botYOUR_BOT_TOKEN/getUpdates
```

Пример:
```
https://api.telegram.org/bot1234567890:ABCdefGHI.../getUpdates
```

Найдите в ответе:
```json
{
  "ok": true,
  "result": [{
    "message": {
      "chat": {
        "id": 123456789,  ← Это ваш Chat ID
        "first_name": "Ваше имя",
        "type": "private"
      }
    }
  }]
}
```

**Chat ID** - это число в поле `chat.id`

---

## 📋 Шаг 3: Проверка

Проверьте, что бот работает:

```bash
# Замените на свои данные
BOT_TOKEN="1234567890:ABCdef..."
CHAT_ID="123456789"

# Отправьте тестовое сообщение
curl -s -X POST "https://api.telegram.org/bot$BOT_TOKEN/sendMessage" \
  -d chat_id=$CHAT_ID \
  -d text="🚀 Тест! Бот работает!"
```

Если всё правильно, вы получите сообщение от бота в Telegram! ✅

---

## 📋 Шаг 4: Добавьте секреты в GitHub

Перейдите: **GitHub → Settings → Secrets and variables → Actions**

Добавьте 2 новых секрета к уже существующим:

### Все необходимые секреты (7 штук):

| Секрет | Значение | Пример |
|--------|----------|--------|
| `SSH_PRIVATE_KEY` | Приватный SSH ключ | `-----BEGIN RSA...` |
| `SSH_HOST` | IP сервера | `89.111.172.122` |
| `SSH_USER` | Пользователь на VPS | `username` |
| `PROJECT_PATH` | Путь к проекту | `/home/username/hakolr.dev` |
| `FRONTEND_URL` | URL сайта | `https://hakolr.dev` |
| **`TELEGRAM_BOT_TOKEN`** ⭐ | Токен бота | `1234567890:ABCdef...` |
| **`TELEGRAM_CHAT_ID`** ⭐ | Ваш Chat ID | `123456789` |

---

## ✅ Готово!

Теперь при каждом деплое вы будете получать уведомления в Telegram:

### 🚀 При начале деплоя:
```
🚀 Начинается деплой hakolr.dev

Ветка: main
Коммит: abc123...
Автор: maksim
```

### ✅ При успешном деплое:
```
✅ Деплой hakolr.dev завершен успешно!

🌐 Сайт: https://hakolr.dev
📦 Контейнеры: postgres: running, backend: running, frontend: running, nginx: running
⏱️ Время: 2024-11-17 23:45:30
```

### ❌ При ошибке:
```
❌ Деплой hakolr.dev завершился с ошибкой!

Проверьте логи: https://github.com/...
```

---

## 🎯 Дополнительные возможности

### Отправка в группу/канал

Если хотите получать уведомления в группе:

1. Создайте группу в Telegram
2. Добавьте бота в группу
3. Сделайте бота администратором (чтобы он мог писать)
4. Получите Chat ID группы:
   ```
   https://api.telegram.org/botYOUR_TOKEN/getUpdates
   ```
   Ищите `chat.id` с отрицательным числом (например, `-987654321`)
5. Используйте это отрицательное число как `TELEGRAM_CHAT_ID`

### Отключение уведомлений о старте

Если не хотите получать уведомление о начале деплоя, удалите в `.github/workflows/deploy.yml`:

```yaml
- name: Send Telegram notification - Start
  run: |
    ...
```

---

## 🔧 Решение проблем

### Бот не отправляет сообщения

1. Проверьте токен:
   ```bash
   curl https://api.telegram.org/botYOUR_TOKEN/getMe
   ```
   Должно вернуть информацию о боте.

2. Проверьте Chat ID:
   - Убедитесь, что написали боту хотя бы одно сообщение
   - Попробуйте получить Chat ID через @userinfobot

3. Проверьте секреты в GitHub:
   - Нет лишних пробелов в начале/конце
   - Токен скопирован полностью

### Сообщения приходят с задержкой

Это нормально, Telegram API может иметь задержку до 30 секунд.

### Бот отвечает "Forbidden: bot was blocked by the user"

Вы заблокировали бота. Разблокируйте его в Telegram и начните диалог заново.

---

## 📱 Примеры уведомлений

### Успешный деплой

<img width="400" alt="Success notification" src="https://via.placeholder.com/400x200/4CAF50/FFFFFF?text=✅+Deploy+Success" />

### Неудачный деплой

<img width="400" alt="Failure notification" src="https://via.placeholder.com/400x200/F44336/FFFFFF?text=❌+Deploy+Failed" />

---

## 🔐 Безопасность

- ✅ Токен бота хранится только в GitHub Secrets
- ✅ Токен никогда не попадает в логи
- ✅ Только вы можете писать боту (бот не публичный)
- ✅ Сообщения видите только вы

---

**Готово!** Теперь вы будете получать уведомления о каждом деплое! 🎉

